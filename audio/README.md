# Dossier /audio

Dépose ici tes fichiers MIDI exportés en .mp3.

Ensuite, dans `data/scores.js`, indique le chemin dans le champ `mp3`, par exemple :

```js
mp3: "audio/mon-morceau.mp3"
```

Si tu n'as pas encore d'enregistrement pour un morceau, laisse simplement :

```js
mp3: null
```
